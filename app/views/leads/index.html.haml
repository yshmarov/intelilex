%h1 Listing subscribers

%table.table
  %thead
    %tr
      /%th{:colspan => "2"}
      %th
      %th Email
      %th Comment
      /%th
      %th
  %tbody
    - @leads.each_with_index do |lead, index|
      %tr
        %td= index +1
        %td= lead.email
        %td= lead.comment
        /%td= link_to '', edit_lead_path(@lead), class: "btn btn-warning fa fa-edit btn-sm"
        %td= link_to 'Delete', lead, method: :delete, data: { confirm: 'Are you sure?' }, class: "btn btn-danger btn-sm"
